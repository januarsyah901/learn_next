'use client';import { useState } from "react";import { IoClose, IoMenu } from 'react-icons/io5';import clsx from "clsx";import Link from "next/link";import { usePathname } from "next/navigation";const links = [    { name: "Home", href: "/" },    { name: "About", href: "/about" },    { name: "Rooms", href: "/room" },    { name: "Contact", href: "/contact" },    { name: "My Reservation", href: "/myreservation" },];const adminLinks = [    { name: "Dashboard", href: "/admin/dashboard" },    { name: "Manage Room", href: "/admin/room" },];const Navlink = () => {    const [open, setOpen] = useState(false);    const pathname = usePathname();    return (        <>            {/* Mobile menu button */}            <button                onClick={() => setOpen(!open)}                className="inline-flex items-center justify-center p-2 text-gray-600 rounded-md md:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-orange-400"                aria-label="Toggle menu"            >                {open ? <IoClose className="size-7 text-orange-400" /> : <IoMenu className="size-7" />}            </button>            {/* Navigation links */}            <div className={clsx(                "w-full md:block md:w-auto transition-all duration-300 ease-in-out",                {                    "block mt-4 absolute left-0 right-0 top-16 bg-white border-b border-gray-100 p-6 shadow-xl": open,                    "hidden": !open                }            )}>                <ul className="flex flex-col md:flex-row md:items-center space-y-2 md:space-y-0 md:space-x-1 lg:space-x-4">                    {/* Main Links */}                    {links.map((link) => {                        const isActive = pathname === link.href;                        return (                            <li key={link.href}>                                <Link                                    href={link.href}                                    onClick={() => setOpen(false)}                                    className={clsx(                                        "block py-2 px-4 text-sm font-semibold transition-colors rounded-md",                                        isActive                                            ? "text-orange-400 bg-orange-50 md:bg-transparent"                                            : "text-gray-600 hover:text-orange-400 hover:bg-orange-50 md:hover:bg-transparent"                                    )}                                >                                    {link.name}                                </Link>                            </li>                        );                    })}                    <div className="hidden md:block h-6 w-px bg-gray-200 mx-2" />                    <div className="md:hidden h-px bg-gray-100 my-2" />                    {/* Admin Links */}                    {adminLinks.map((link) => {                        const isActive = pathname === link.href;                        return (                            <li key={link.href}>                                <Link                                    href={link.href}                                    onClick={() => setOpen(false)}                                    className={clsx(                                        "block py-2 px-4 text-sm font-semibold transition-colors rounded-md",                                        isActive                                            ? "text-orange-400 bg-orange-50 md:bg-transparent"                                            : "text-gray-600 hover:text-orange-400 hover:bg-orange-50 md:hover:bg-transparent"                                    )}                                >                                    {link.name}                                </Link>                            </li>                        );                    })}                    {/* Sign In Button */}                    <li className="pt-2 md:pt-0 md:ml-4">                        <Link                            href="/signin"                            onClick={() => setOpen(false)}                            className="block w-full md:w-auto px-6 py-2.5 text-center text-sm font-bold text-white bg-orange-400 rounded-lg hover:bg-orange-500 transition-all shadow-md hover:shadow-orange-200 active:scale-95"                        >                            Sign In                        </Link>                    </li>                </ul>            </div>        </>    );};export default Navlink;